import 'package:flutter/material.dart';

class WebmainPge extends StatelessWidget {
  const WebmainPge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}